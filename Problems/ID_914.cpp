#include <vector>
#include <map>
#include <stdio.h>

using namespace std;
class Solution {
public:
    bool hasGroupsSizeX(vector<int>& deck) {
        int n = deck.size();
        map<int,int> count;        
    
    }
};